# Generated by IBM TransformationAdvisor
# Wed Jul 10 14:27:12 UTC 2019

apiVersion: v1
kind: Service
metadata:
  name: loyaltylevel10war
  labels:
    app: loyaltylevel10war
spec:
  ports:
    - port: 9080
  selector:
    app: loyaltylevel10war
  type: NodePort

---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    app: loyaltylevel10war
  name: loyaltylevel10war
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: loyaltylevel10war
    spec:
      containers:
      - image: mycluster.icp:8500/default/loyaltylevel10war:latest
        name: loyaltylevel10war
        env:
          - name: WLP_LOGGING_CONSOLE_FORMAT
            value: json
          - name: WLP_LOGGING_CONSOLE_LOGLEVEL
            value: info
          - name: WLP_LOGGING_CONSOLE_SOURCE
            value: message,trace,accessLog,ffdc
        ports:
        - containerPort: 9080
          protocol: TCP
